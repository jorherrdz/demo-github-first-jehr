#view teh code if mtcar dataset
head(mtcars)

#calculate the mean mpg all cars and save it as a variable
mean_mpg <- mean(mtcars$mpg)

#print the mean mpg
mean_mpg

#make the mean mpg divided into 2

mean_2 <- mean_mpg/2
